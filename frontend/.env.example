# Base URL of the ERP Arpia API. Vite loads env files per mode:
#   .env.development -> `npm run dev` (localhost backend)
#   .env.production  -> `npm run build` (prod backend)
# Copy this file to .env.local to override locally (never commit it).
VITE_API_BASE_URL=http://localhost:8000/api/v1
