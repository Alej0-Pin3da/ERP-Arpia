# Dependencies
node_modules/

# Build output
dist/
coverage/

# Local env overrides — never commit
.env.local
.env.*.local

# Env files below ARE committed (the root .gitignore ignores `.env.*`, so
# re-include the documented examples explicitly).
!.env.example
!.env.development
!.env.production
