# Herramientas de orquestación local (registro de skills por máquina)
.atl/
.pi/

# Python / FastAPI
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Secretos y entorno
.env
.env.*

# Node (frontend futuro)
node_modules/
dist/

# Docker
postgres_data/

# IDE local (WebStorm/PyCharm)
.idea/

# Datos de negocio locales (hoja de cálculo de trabajo, no código)
ARPIA.xlsx
ARPIA - INVERSION VALQUI (1).csv
*.csv
# Dump local de DB (lo canónico son las migraciones alembic, no el dump)
DDL.sql
# Reportes generados por el pipeline de migración
backend/migrate/reports/
# CodeGraph local index (machine-local, regenerable)
.codegraph/

# Logs efímeros de verify/build (no commitear)
backend_tests*.txt
build_output.txt
frontend_tests.txt
log.txt
backend_tests_legacy.txt
backend_tests_v4_output.txt
